# 工程规范整改记录（2026-08-09）

## 本轮结论

新版规范第一阶段和第二阶段的可立即执行项已落地，并进入根目录 `npm run check`。后续提交无法绕过格式、语义、文件规模、SQL 分层、包依赖、类型、测试和生产构建门禁。

## 已完成

- 建立 Prettier、ESLint、React Hooks 和类型感知 Promise 检查。
- 建立统一 `tsconfig.base.json`，启用严格模式、未知索引检查、覆盖声明检查和 Switch 穿透检查。
- 修复严格类型检查发现的空工作区、数据库值、编辑器节点和同步数组索引风险。
- 建立包依赖方向、内部依赖声明、循环依赖和统一 TypeScript 基线门禁。
- 将业务层内嵌 SQL 清零，禁止仓储内嵌 SQL 文本和 `SELECT *`。
- 为 Electron IPC Handle 与 Listener 统一校验调用来源、请求结构和响应结构。
- 拆出数据库页面挂载、创建提示、名称编辑和视图默认配置模块。
- 新文件限制为 500 行；现有大文件冻结当前预算且绝对不得超过 1000 行。
- 增加 PR 安全、数据、测试与恢复检查模板，以及 UTF-8/LF 仓库约束。

## 自动验收

根目录 `npm run check` 顺序执行：

1. Prettier 格式检查；
2. ESLint 语义和 Promise 安全检查；
3. 文件行数预算检查；
4. SQL 分层检查；
5. 包边界与循环依赖检查；
6. 所有工作区 TypeScript 检查；
7. 所有工作区测试；
8. 桌面端生产构建。

本轮验收结果：158 项测试通过，所有门禁和生产构建通过。

## 后续渐进整改

以下内容按规范第 19 章继续拆分执行，不能与普通功能修改混在同一提交：

- 将 Electron 启动边界之外的 CJS 业务模块逐步迁移为可构建的 TypeScript 包。
- 继续拆分冻结预算中的大文件，优先处理 `DatabaseBlock.tsx`、`App.tsx` 和 `main.cjs`。
- 为全部 IPC Channel 建立共享的逐通道请求/响应类型与 Schema，而不仅是公共结构校验。
- SQLite 时间字段改为 UTC Unix 毫秒前，先完成追加迁移、备份恢复和兼容切换方案。
- 目标发布栈启用时，增加 Playwright 核心桌面旅程、迁移恢复和 Electron 安全发布门禁。
